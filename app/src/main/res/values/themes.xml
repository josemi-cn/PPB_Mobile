<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.ProjectPhoneBasket" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.ProjectPhoneBasket" parent="Base.Theme.ProjectPhoneBasket">
        <item name="android:actionMenuTextAppearance">@color/white</item>
    </style>

    <style name="Theme.ProjectPhoneBasket.Menu" parent="TextAppearance.AppCompat.Widget.ActionBar.Menu">
        <item name="android:actionMenuTextAppearance">@color/white</item>
        <item name="android:background">@color/background_color</item>
        <item name="android:titleTextColor">@color/white</item>
        <item name="android:iconTint">@color/white</item>
    </style>

    <style name="Theme.Popup" parent="Base.Theme.ProjectPhoneBasket">
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowCloseOnTouchOutside">true</item>
    </style>
</resources>